sudo: false

language: node_js

# test on these node.js versions
node_js:
  - 5

before_script:
  # Run lint on every commit
  - npm run build
  - npm run lint

script: "npm run test:cross-browser"

notifications:
  email:
    recipients:
      - seth.yates@krux.com
    on_success: [change]
    on_failure: [always]
